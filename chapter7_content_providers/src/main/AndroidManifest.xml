<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cvora.chapter7_content_providers">

    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ContactsContentProvider" />

        <provider
            android:name=".BookContentProvider"
            android:authorities="cvora.chapter7_content_providers.books" />

        <activity android:name=".UsingOwnContentProvider_Activity"></activity>
    </application>

</manifest>